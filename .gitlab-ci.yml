build site:
  image: node:14.21.3
  stage: build
  script:
    - npm install --progress=false
    - npm run build
  artifacts:
    expire_in: 1 week
    paths:
      - dist

deploy:
  stage: deploy
  tags:
    - deploy
  script:
    - rsync -rav --delete dist/ root@nginx.hel.local:/var/www/domiibunn.live
